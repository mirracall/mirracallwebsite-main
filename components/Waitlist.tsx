"use client";
import { FormEvent, useState } from "react";
export function Waitlist() {
  const [state, setState] = useState<'idle' | 'loading' | 'success' | 'error'>('idle');
  function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setState('loading'); window.setTimeout(() => setState('success'), 550); }
  return <section className="waitlist section" id="waitlist"><div className="container waitlist-grid"><div><p className="eyebrow">Early access</p><h2>Be among the first to experience your own Mirra Call.</h2><p>Mirra Call is currently in development. Join the waitlist for product updates, pilot opportunities, and early access.</p></div><form onSubmit={submit} aria-describedby="form-status"><div className="form-row"><label>First name<input name="firstName" autoComplete="given-name" required /></label><label>Last name<input name="lastName" autoComplete="family-name" required /></label></div><label>Email<input type="email" name="email" autoComplete="email" required /></label><label>I am a…<select name="userType" defaultValue=""><option value="" disabled>Select one (optional)</option><option>Patient / user</option><option>Caregiver</option><option>Occupational therapist</option><option>Physiotherapist</option><option>Clinician</option><option>Researcher</option><option>Healthcare organization</option><option>Other</option></select></label><label>What interests you about Mirra Call?<textarea name="interest" rows={3}></textarea></label><button className="button" type="submit" disabled={state === 'loading'}>{state === 'loading' ? 'Joining…' : 'Join the waitlist'}</button><p id="form-status" className={`form-status ${state}`} aria-live="polite">{state === 'success' && 'Thanks — this prototype is ready to connect to your waitlist endpoint.'}{state === 'error' && 'Something went wrong. Please try again.'}</p><p className="form-note">Prototype form only. No information is transmitted or stored yet.</p></form></div></section>;
}
